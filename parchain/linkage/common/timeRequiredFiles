BNCHMRK = linkage

# generic toplevel common files needed by this and most timing code 
GLOBAL_GENERIC = utils.h parseCommandLine.h IO.h parallel.h gettime.h sequence.h 

# toplevel common files needed by the timing code
TIME_GLOBAL_REQUIRE = geometry.h geometryIO.h $(GLOBAL_GENERIC) 

# local common files needed by the timing code
TIME_REQUIRE = linkage.h

